#!/usr/bin/expect -f
set timeout 600
spawn ssh -o StrictHostKeyChecking=no -o PubkeyAuthentication=no root@72.61.231.187
expect "password:"
send "Photoshop09@\r"
expect "# "
send "cd /root/grekam-os && git pull origin main && pnpm --filter @grekam/db exec prisma db push && cd apps/api && pnpm build && pm2 restart grekam-os-api && cd ../web && pnpm build && pm2 restart grekam-os-web\r"
expect "# "
send "exit\r"
expect eof
