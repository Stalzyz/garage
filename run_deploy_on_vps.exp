#!/usr/bin/expect -f
set timeout 300
spawn ssh -o StrictHostKeyChecking=no root@72.61.231.187
expect "assword:"
send "Photoshop09@\r"
expect "# "
send "cd /root/grekam-os && git pull origin main && pnpm install && pnpm --filter=web build && pm2 restart grekam-os-web\r"
expect "# "
send "exit\r"
expect eof
